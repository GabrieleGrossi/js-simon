/*const clock = setInterval(lesson, 1000);

function lesson() {
    alert('New lesson');
    clearInterval(clock);
}
alert('test')
*/

function updateCountdown() {
    const now = new Date();
    const alarm = new Date();

    alarm.setHours(9);
    alarm.setMinutes(30);
    alarm.setSeconds(0);

    
    }
  
    updateCountdown();